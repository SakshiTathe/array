num=[2,7,11,15]
target=9
mapdict={}
for i in range(len(num)):
    secnum=target-num[i]
    if(secnum in mapdict):  #check the values
        print(i,mapdict[secnum])   # retrive index
        break 
    mapdict[num[i]]=i        #map the number to its index
