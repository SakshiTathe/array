nums = [3,1,-2,-5,2,-4]
result=[]
pi=1
ne=0
i=0
if nums[0]>=0:
    pi=0
    ne=1
while i<len(nums):
    if nums[i]>=0:
        result.insert(pi,nums[i])
        pi+=2
    else:
        result.insert(ne,nums[i])
        ne+=2
    i+=1
print(result)
